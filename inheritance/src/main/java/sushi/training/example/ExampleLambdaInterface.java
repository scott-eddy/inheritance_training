package sushi.training.example;

/*
 * Here we're going to define a an interface to a function.
 * 
 * Interfaces are like classes, except that you can't make an instance of them
 */
public interface ExampleLambdaInterface
{
    // A function with no parameters
    void no_param_function();

    // A function with a single parameter
    // Note: only enable this once you've played with with no_param_function()
    //void single_param_function(int x);

}